# 작업자 안전 모니터링 시스템 요구사항 명세서

## 1. 서론

### 1.1 목적

작업자의 위험 상황을 감지하고 관리자에게 알리는 시스템의 MVP 요구사항을 정의한다.

### 1.2 범위

작업자 앱, 서버, 관리자 웹을 대상으로 낙상·생체신호 이상·위험구역 접근 감지를 구현한다. 실내 측위 및 지도상의 작업자 위치 표시는 제외한다.

### 1.3 용어 및 약어

- MVP: 핵심 기능을 구현한 최소 제품.
- BLE: Bluetooth Low Energy. 비콘 기반 접근 감지에 사용한다.
- 위험 이벤트: 감지 모듈이 생성하는 위험 상황 정보.

### 1.4 참고자료

None

## 2. 시스템 개요

### 2.1 제품 관점 및 주요 기능

작업자 앱이 센서·워치·비콘 데이터 등으로 위험 상황을 분석하고, 서버를 통해 관리자 웹에 위험 정보를 전달한다.

### 2.2 사용자 유형

- 작업자: 앱에서 모니터링을 시작·종료한다.
- 관리자: 작업자 상태 및 위험 알림·이력을 확인한다.

### 2.3 운영 환경

Android/Kotlin 앱, 갤럭시 워치, BLE 비콘, 서버 및 웹 브라우저로 구성한다.

### 2.4 제약사항

학부생 5명이 한 학기 동안 구현하는 MVP로 범위를 제한한다.

### 2.5 가정 및 의존성

센서 권한, 워치 연결, 비콘 설치 및 서버 통신이 필요하다. 생체신호 수집 가능 항목은 대상 워치에서 확인 후 확정한다.

## 3. 기능 요구사항

| ID | 요구사항 |
| --- | --- |
| FR-01 | 작업자는 모니터링을 시작·종료할 수 있어야 한다. |
| FR-02 | 앱은 스마트폰 가속도 센서 및 자이로 센서로 낙상을 판단하고 위험 이벤트를 생성해야 한다. |
| FR-03 | 앱은 워치 생체신호가 설정된 정상 범위를 벗어나면 위험 이벤트를 생성해야 한다. |
| FR-04 | 앱은 BLE 비콘으로 지정 위험구역 접근을 판단하고 위험 이벤트를 생성해야 한다. |
| FR-05 | 공통 판단 모듈은 위험 이벤트와 설정된 조건에 따라 서버 알림 송신 여부를 결정해야 한다. |
| FR-06 | 앱은 작업자 식별자와 위험 이벤트를 서버로 전송해야 한다. |
| FR-07 | 서버는 위험 이벤트를 검증·저장하고 관리자 웹에 전달해야 한다. |
| FR-08 | 관리자 웹은 작업자 상태, 수집된 생체신호, 신규 위험 알림 및 이력을 제공해야 한다. |

## 4. 외부 인터페이스 요구사항

### 4.1 사용자 인터페이스

앱은 모니터링 시작·종료 및 상태 표시를, 관리자 웹은 상태·알림·이력 조회를 제공한다.

### 4.2 하드웨어 인터페이스

스마트폰 가속도 센서, 갤럭시 워치, BLE 비콘을 연동한다.

### 4.3 소프트웨어 인터페이스

앱과 관리자 웹은 서버 API를 이용한다. 내부 감지 모듈의 공통 계약은 6.5절에 정의한다.

### 4.4 통신 인터페이스

앱과 서버는 HTTP 기반 REST API 및 JSON으로 통신한다. 상세 API 규격은 구현에 따라 확정한다.

## 5. 데이터 요구사항

| 데이터 | 필수 항목 |
| --- | --- |
| 위험 알림 | 작업자 식별자, 위험 유형, 발생 시각, 설명 |
| 생체신호 | 작업자 식별자, 측정 항목, 값, 단위, 측정 시각 |

위험 알림은 이력 조회가 가능하도록 저장한다. 생체신호 저장 여부와 데이터 보관 기간은 별도 확정한다.

## 6. 비기능 요구사항

### 6.1 성능

위험 알림은 발생 후 관리자 웹에 자동 반영되어야 한다. 허용 지연시간은 구현에 따라 확정한다.

### 6.2 보안

관리자 데이터 접근에는 사용자 인증을 적용하고, 앱·서버 통신에는 HTTP를 사용한다.

### 6.3 신뢰성 및 가용성

센서·워치 미연결 또는 신호 미수신 상태를 정상 측정 상태와 구분하여 표시해야 한다.

### 6.4 사용성

작업자는 모니터링 실행 여부를, 관리자는 위험이 발생한 작업자와 위험 유형을 식별할 수 있어야 한다.

### 6.5 유지보수성 및 이식성

감지 모듈은 `SafetyDetector`를 구현하고 `SafetyEvent`를 전달한다. `SafetyManager`가 모듈 관리와 알림 송신 판단을 담당한다. Android 외 플랫폼 이식은 MVP 범위에서 제외한다.

## 7. 검증 및 인수 기준

| 검증 | 통과 기준 |
| --- | --- |
| 모니터링 제어 | 시작 시 감지를 수행하고 종료 시 감지를 중단한다. |
| 위험 판단 | 각 기능의 정상·위험 입력에 대해 설정 조건에 맞게 이벤트와 송신 여부가 결정된다. |
| 알림 전달 | 전송한 작업자·위험 정보가 관리자 웹과 이력에 일치하게 표시된다. |
| 상태·접근 확인 | 생체신호 및 미연결 상태가 구분되고, 인증 없는 관리자 데이터 접근이 거부된다. |

## 8. 요구사항 추적성

| 요구사항 | 검증 항목 |
| --- | --- |
| FR-01 | 모니터링 제어 |
| FR-02~05 | 위험 판단 |
| FR-06~08 | 알림 전달 |
| FR-08, 6.2~6.4 | 상태·접근 확인 |

## 9. 부록

- 미정: 감지 임계값·지속시간, 생체신호 항목 및 워치 연동 범위, API 규격, 알림 허용 지연시간, 데이터 보관 정책.
